# Arbetsström 15 — Finland (Eduskunta, Finlex)
# Installera: pip install -r requirements.txt

# MCP-server
mcp>=1.0.0

# HTTP-anrop mot api.eduskunta.fi och opendata.finlex.fi
httpx>=0.27.0

# XML-parsning (Finlex AKN XML via lxml)
lxml>=5.0.0

# HTML-parsning (Eduskuntas HTML-fulltextsidor)
beautifulsoup4>=4.12.0

# Konfiguration via .env
python-dotenv>=1.0.0

# Databas — välj ett:
# Alternativ A: PostgreSQL + pgvector (krävs för semantisk sökning)
psycopg2-binary>=2.9.0
# Alternativ B: SQLite (ingen serverinstallation)
# sqlite-vec installeras separat: pip install sqlite-vec

# HTTP-transport (krävs för MCP_TRANSPORT=http)
starlette>=0.40.0
uvicorn>=0.30.0

# --- Semantisk sökning och RAG ---
# Tvåspråkig embedding: finska (TurkuNLP) + svenska (KBLab)
sentence-transformers>=3.0.0

# Språkdetektering (finska vs. svenska i sökfrågor)
langdetect>=1.0.9

# Query-expansion via OpenAI-kompatibel endpoint (valfritt)
openai>=1.0.0
